node_modules/
dist/
.output/
.wxt/
*.zip
.DS_Store

# local scratch
.audit_out.txt
.cctmp/

# extension signing key — only needed for .crx signing, never commit
*.pem

# stray media downloads (keep out of the repo)
*.webm
*.part
*.mp4
*.mov

# Vercel CLI project link (contains project/org ids)
.vercel/

# Scratch worktrees created by agent sessions
.claude/worktrees/
